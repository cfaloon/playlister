<% content_for :heading, 'All Albums' %>


<div class="card-columns">
  <% @albums.each do |album| %>

    <div class="card">
      <div class="card-body">
        <h4><%= link_to album.name, album_path(album) %></h4>
      </div>
    </div>
  
  <% end %>
</div>

<%= paginate @albums %>
